# Generated by ansible.
{% for other_ip in other_ips %}

conn overcloud-tunnel-{{ loop.index0 }}
    left={{ current_ip }}
    right={{ other_ip }}
    authby=secret
    auto=start
{% endfor %}
{% if pacemaker_running.stdout == 'active' %}
{% for other_ip in other_ips %}

conn overcloud-vip-tunnel-{{ loop.index0 }}
    left={{ internal_api_vip }}
    right={{ other_ip }}
    authby=secret
    auto=start
{% endfor %}
{% endif %}

conn overcloud-vip-tunnel
    left={{ internal_api_vip }}
    right={{ current_ip }}
    authby=secret
    auto=start
